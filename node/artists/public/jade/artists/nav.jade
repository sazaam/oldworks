.nav.zindex10.Vmargin	- var TITLE = 'The StrawNode <sup class="sizeXSm">TM</sup>  Project' ;	ul.txtR		- var st = response.id == '' ? response.parentStep : response , arr = [] ;		- while(!!st.parentStep){			- var path = {id:st.parentStep.id, path:st.parentStep.path} ;			- arr.push(path) ;			- st = st.parentStep 		- }		- arr.reverse() ;		- for(var i = 0 , l = arr.length ; i < l ; i++){			- var path = arr[i] ;			li				a.item.dispBlock.sizeR(href="#/en#{path.path == '@' ? '/' : path.path + '/' }")= path.id == '@' ? TITLE : path.id		- }