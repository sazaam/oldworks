ul#sections.txtL	- sections = response.alphachildren ;	- for(var s in sections) {		- var res = sections[s] ;		- if(res.id == '') continue ;		li			a.item.rel.dispBlock.sizeR(href="#/en#{res.path + '/'}")= res.id				span.arrowindic.abs.dispNone.left0.top0.bottom0 &gt;	- }		- if(!/msie/i.test(navigator.userAgent)) {		script(type="text/javascript")			| $('#sections li a:first').each(function(i, el) {			| 	el.focus() ;			| } ) ;	- }