package modules.patterns
{
	import modules.foundation.Cloneable;
	import modules.foundation.Dispose;
	import modules.foundation.Equal;
	import modules.foundation.Hashable;
	import modules.foundation.VO;

	public interface Prototype  extends Cloneable, Dispose, Equal, Hashable, VO
	{
		
	}
}